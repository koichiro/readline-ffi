= Readline-ffi

GNU Readline Wrapper for Ruby-FFI(JRuby-FFI/Rubinus-FFI/Ruby-FFI).

CRuby's Readline extention intaerface compatiblity.

tested only JRuby Windows environment.

I hope supoorts environments to Mac OSX and Linux.
